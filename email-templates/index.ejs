<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="en-GB">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <title>Paisley Daily Digest</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #fafafa;
            color: #333;
        }
        table { border-spacing: 0; width: 100%; }
        .main { max-width: 700px; margin: auto; background: #fff; border-radius: 6px; overflow: hidden; }
        .header { background: #ff6f3c; color: #fff; padding: 20px; }
        .header h1 { margin: 0; font-size: 28px; }
        .date { font-size: 14px; margin-top: 5px; opacity: 0.9; }
        .section-title { background: #f2f2f2; padding: 12px 20px; font-size: 18px; font-weight: bold; border-bottom: 1px solid #ddd; }
        .post { padding: 16px 20px; border-bottom: 1px solid #eee; }
        .post h2 { margin: 0 0 6px; font-size: 16px; }
        .post h2 a { text-decoration: none; color: #0073e6; }
        .meta { font-size: 12px; color: #666; margin-top: 6px; }
        .footer { text-align: center; font-size: 12px; color: #777; padding: 20px; }
        .footer a { color: #ff6f3c; text-decoration: none; }
    </style>
</head>
<body>
<table class="main">
    <tr>
        <td class="header">
            <h1>Paisley <%= data.name %></h1>
            <div class="date">
                <%= data.date.weekDay %>, <%= data.date.month %> <%= data.date.date %>, <%= data.date.year %>
            </div>
        </td>
    </tr>

    <% data.mails.forEach(entry => { %>
        <tr>
            <td>
                <div class="section-title"><%= entry.config.name %></div>
                <% entry.posts.forEach((post, idx) => { %>
                    <div class="post">
                        <h2>
                            <a href="<%= post.link %>"><%= post.title %></a>
                        </h2>
                        <div class="meta">
                            <strong><%= post.points %></strong> points •
                            <strong><%= post.comments %></strong> comments •
                            by <%= post.author %> •
                            <a href="<%= post.commentLink %>">HN discussion</a> •
                            <%= post.domain %>
                        </div>
                    </div>
                <% }) %>
            </td>
        </tr>
    <% }) %>

    <tr>
        <td class="footer">
            <p>
                Generated with ❤️ by <strong>Paisley Digest</strong>
            </p>
<!--            Need to make features better?-->
<!--            <p>-->
<!--                <a href="https://news.ycombinator.com/news">Visit Source</a> •-->
<!--                <a href="#">Unsubscribe</a> •-->
<!--                <a href="#">Feedback</a>-->
<!--            </p>-->
        </td>
    </tr>
</table>
</body>
</html>
